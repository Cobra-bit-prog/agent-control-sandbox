# Agent Control — sandbox

This is a **sandbox** copy of Agent Control with the V1 **approval inbox**.

- **Not production.** The live site (agent-control.net / agent-guard) is unchanged.
- Sign in → **Inbox** (`/inbox`) from the console nav.
- Off-policy sends **hold** until you Allow once / Always allow / Block (10-minute expiry).
- Trial is **1 day (24 hours)**.

See [SANDBOX.md](./SANDBOX.md) for local run instructions and a manual Inbox QA checklist.

Product name: **Agent Control** (repo `agent-guard` is the same product; old code name).
