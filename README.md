# Agent Control — sandbox

This is a **sandbox** copy of Agent Control with the V1 **approval inbox**.

- Not production. The live site is unchanged.
- Sign in → **Inbox** (`/inbox`).
- Off-policy sends **hold** until you Allow once / Always allow / Block.

Product name: **Agent Control** (repo `agent-guard` is the same product; old code name).
